package dungeonmania.entities.logical;

import dungeonmania.entities.rules.Rule;
import dungeonmania.util.Position;

public class LightBulb extends LogicalEntity {
    public LightBulb(Position position, Rule rule) {
        super(position, rule);
    }
}
